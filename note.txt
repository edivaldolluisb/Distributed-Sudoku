pensando em um método de resolução, acho que seja melhor ter uma lista com os espaços vazios a serem completos e os nodes vão pegando os espaços vazios e completando os serus respetivos valores, enquanto um node estiver trabalhando em um espaço vazio, os outros nodes não podem trabalhar nele, para evitar que dois nodes trabalhem no mesmo espaço vazio ao mesmo tempo, acho que a melhor forma de fazer isso é ter um lock para cada espaço vazio, e os nodes só podem trabalhar em um espaço vazio se conseguirem o lock, se não conseguirem, eles tentam pegar outro espaço vazio, se não conseguirem pegar nenhum espaço vazio, eles esperam até que um node libere um espaço vazio, acho que essa é a melhor forma de fazer isso, mas não tenho certeza se é a melhor forma de fazer isso, então vou tentar implementar isso e ver se funciona. 


O que acontece se um node desconectar e não cnoseguir remover o lock de um espaço vazio? acho que a melhor forma de resolver isso é ter um timeout para o lock, se o node não conseguir remover o lock em um certo tempo, o lock é removido automaticamente.




# usar connect em uma thread separada, ou ter só o bind e ter a lista com os nodes da rede e enviar mensagem para todos os nodes da rede ?
Um vantagem de ter o connect é que eu posso detectar quando um node desconectar